spring.application.name=oms-backend
server.port=${PORT:8080}

# ---------------- MySQL (Railway) ----------------
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=KKsGGRlHLqZFSaUStFuTdCNIVkkuUpdx
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------------- JPA / Hibernate ----------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ---------------- File storage (invoices, product images) ----------------
app.upload.invoice-dir=uploads/invoices
app.upload.product-image-dir=uploads/products
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ---------------- JWT ----------------
app.jwt.secret=oms-super-secret-key-change-this-in-production-min-256-bits-long
app.jwt.expiration-ms=86400000

# ---------------- CORS ----------------
app.cors.allowed-origins=http://localhost:4200,https://online-management.netlify.app

# ---------------- Logging ----------------
logging.level.root=INFO
logging.level.com.oms.backend=DEBUG
logging.level.org.springframework.security=DEBUG
